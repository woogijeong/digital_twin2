# Ralph progress — Neuromeka Indy7 Digital Twin (P0 MVP)

Plan: .omc/plans/2026-09-03-neuromeka-digital-twin-p0.md
PRD:  .omc/prd.json
Design: Direction A "Control Room" (dark HMI) — plan section 8, design/Main.dc.html

## Environment (this machine)
- node v24.19.0, pnpm 11.24.0, npm 11.17.0
- python 3.14.6 present BUT backend pinned to 3.12.13 via `uv python pin 3.12`
  (neuromeka 3.4.2.2 -> grpcio-tools 1.59 has no 3.14 wheel / needs pkg_resources)
- uv 0.11.33, git 2.54.0.windows; Windows 11, PowerShell primary
- `make` NOT installed -> repo-root `pnpm dev|test|assets` are the entry points
- No controller access from this session -> mock mode is the automated proof path

## Status: ALL 9 STORIES PASS. Ready for Step 7 reviewer verification.

## Iteration log

### Iteration 1 (2026-09-03/04)
- git init (branch main), first commit 57bb8b7 (PRD + design directions).
- Wrote .omc/prd.json with 9 task-specific stories (US-001..US-009).
- US-001 scaffolding + neuromeka gate:
  - backend: uv project, Python 3.12 pin, FastAPI app (config, schemas, robot/, api/, main).
  - RK4 resolved by pinning Python 3.12 (Windows native, no WSL2).
  - frontend: Vite React-TS + three@0.169.0 + urdf-loader@0.12.6 (exact pins).
  - root package.json: `dev` (concurrently api+web), `assets`, `test*`. Makefile mirrors.
  - Criterion amendment recorded: Makefile -> pnpm scripts (make not installed).
- US-002 asset pipeline: scripts/fetch_urdf_assets.py -> frontend/public/robot/
  indy7.urdf (mesh paths rewritten relative) + 7 STL meshes + NOTICE.md. Verified 0 abs paths.
- US-003 RobotService: base.py ABC, indy.py (forces set_simulation_mode(True),
  asyncio.to_thread, RobotUnavailable), mock.py (deterministic sine + planar FK/IK stand-in),
  factory.py (real -> mock fallback). tests: sim-mode gate, fallback, AST scan for movej/movel.
- US-004 REST: /api/health /pose /state, POST /api/ik (422 on IkFailed). 16 pytest, 90% cov.
- US-005 telemetry WS: TelemetryHub (1 task, per-client Queue(maxsize=1) drop-oldest). 4 ws tests.
- US-006 3D viewer: RobotViewer.tsx (scene/camera/OrbitControls/grid/lights/resize),
  urdfRobot.ts (URDFLoader, setJoints deg->rad, Z-up->Y-up), jointTween.ts (cubic, cancellable).
  urdf-loader has no types -> src/types/urdf-loader.d.ts.
- US-007 panel: StatusBar, PosePanel (TCP POSE 3x2 live + TARGET POSE seed-once + SYNC + APPLY),
  JointBars, ViewportOverlays (TCP text, XYZ triad, hint). theme.ts = Direction A tokens.
  Criterion amendment recorded: target inputs seed-once (20Hz clobber race found via Playwright).
- US-008 mirroring: useJointAnimation (telemetry follow, tween wins during IK move).
  docs/hardware-check.md manual checklist.
- US-009 e2e: frontend/e2e/twin.spec.ts (load->mirror->apply->reject->no-JS-errors).
  README.md (one-command run, ASCII arch, 3 screenshots in docs/screenshots/).
- `pnpm test` green: 16 pytest + frontend build + 1 Playwright e2e.

## Known facts / patterns
- IndyDCP3 SDK: get_control_data()->{q[6] deg, p[6] [mm,mm,mm,deg,deg,deg]},
  inverse_kin(tpos,init_jpos)->{jpos,response{code,msg}}, forward_kin(jpos)->{tpos},
  set_simulation_mode(bool). movej/movel = P1 only, banned in P0 (AST test enforces).
- pytest from repo root needs `uv run --directory backend pytest` (not --project) so
  backend/pyproject.toml [tool.pytest.ini_options] asyncio_mode=auto is picked up.
- uvicorn --reload triggers WinError 10013 on this box -> dev:api has no --reload
  (dev:api:reload exists with --reload-dir backend/app for those who want it).
- Playwright webServer reuseExistingServer:!CI can serve a STALE backend locally;
  run e2e with CI=1 or kill :8000 first for accurate screenshots.
- Euler angles: controller raw values passed through unchanged (plan decision 3).

## Review round 1 (architect, opus) — REJECTED narrowly, commit 78e16bc
- D1 HIGH: telemetry hub exited loop on any stream error -> UI frozen at LINKED forever.
- D2 MED: header host hardcoded '192.168.3.4', gated on model name; lies under INDY_HOST override.
- D3 MED: no-motion AST scan can't see getattr(self._indy, name) dynamic dispatch -> vacuous.
- D4 LOW(blocking): 7 hardcoded hexes outside theme.ts (US-007 c4 literal fail).
- D5-D9 non-blocking: three.js geometry not disposed; jointTween cancel contract false;
  dead _offset; weak e2e asserts; package-lock.json stray + claude.md stale ref.

## Review round 1 fixes — commit 0e74650
- D1: hub retry-with-backoff + _STOP sentinel closes sockets; client staleness watchdog
  (1.5s) -> 'reconnecting'; stale dims PosePanel + ViewportOverlays. New ws regression test.
- D2: /api/health exposes host; StatusBar shows real host + "(not dialled)" in mock.
- D3: _ALLOWED_SDK_CALLS frozenset in _call() + test that _call("movej") raises; AST scan
  extended to ast.Constant strings.
- D4: axis/badge/hint/onTeal tokens in theme.ts.
- D5: dispose STL geo+mats, tcpMarker, grid. D6: jointTween reject hoisted. D7: _center
  re-set by solve_ik (mock target sticks). D8: e2e asserts joints moved >3deg to solution.
- D9: package-lock.json removed (pnpm only), pnpm: in scripts, claude.md + NOTICE.md fixed.
- backend 18 pytest / frontend build 0 err / e2e pass.

## Review round 2 (architect, opus) — APPROVED, commit 0e74650
- All 4 blocking defects verified fixed by the reviewer's own adversarial probes
  (repeated-error stream -> 41 frames/4s; shutdown -> client WebSocketDisconnect;
  INDY_HOST=10.0.0.5 -> health.host="10.0.0.5"; _call("movej") -> BLOCKED;
  0 hex literals outside theme.ts). 18 pytest, coverage 91%, build clean, e2e pass.
- Residual non-blocking: N1 half-open socket (fixed: watchdog now ws.close()),
  N2 uncapped retry log, N3 mock/real solve_ik divergence (documented),
  N4 relaxed health test, N5 .coverage tracked (fixed: gitignored).

## Deslop pass (Ralph 7.5) — commit pending
- Dead code removed: StateResponse.manipulability (always None); get_control_state
  from _ALLOWED_SDK_CALLS (never called); theme.fontUi (unused); client.ts getPose
  + poseToTuple + Pose interface (unused - pose comes from telemetry WS).
- Duplication removed: fmt() defined in PosePanel + ViewportOverlays -> src/format.ts fmt1().
- Simplified: mock.solve_ik clamp-then-check -> plain range check (equivalent).
- N1/N5 fixes folded in (watchdog ws.close(), .coverage gitignored).
- Net -16 lines. Regression: 18 pytest, build 0 err, e2e pass, oxlint 1 warn
  (deliberate seed-once effect - architect-acknowledged).

## STATUS: COMPLETE. All 9 stories pass + architectVerified. Ready for /cancel.
